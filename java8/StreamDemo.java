package main.java8;

import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.IntStream;

public class StreamDemo {

	public static void main(String[] args) {
//		List<String> nameList = Arrays.asList("Bharath","Shubham",
//				"Poojesh","Chandrakala");
//		//converting to upper case
//		List<String> outputList = nameList.stream()
//				.map(s ->s.toUpperCase()).collect(Collectors.toList());
//		System.out.println(outputList);
//		//filtering and converting to upper case
//		List<String> responseList = nameList.stream().filter(s-> s.length()>7)
//				.map(s ->s.toUpperCase()).collect(Collectors.toList());
//		System.out.println(responseList);
//		//filtering and finding the count
//		long nameCount = nameList.stream().filter(s-> s.length()>7)
//				.count();
//		System.out.println(nameCount);
//
//		//		 1. Integer Stream
//		IntStream.range(1, 10) // 10 not included
//		.forEach(System.out::print); //terminal operation, method references
//System.out.println();
//		// 2. Integer Stream with skip
//	IntStream.range(1, 10).skip(3)  //skipping till that element
//							.forEach(x -> System.out.println(x));
	// 3. Integer Stream with sum
//	System.out.println(IntStream.range(1, 5) .sum()); //1+2+3+4
//	System.out.println();
	}

}
